---
import CmsDashboard from "../../components/CmsDashboard.tsx";
import { readAdminSession } from "../../utils/admin-session.ts";
import { mediaAssets } from "../../utils/images.ts";
import "../../styles/cms-dashboard.css";

// The CMS dashboard is a runtime surface: it checks the admin session before
// hydration so GitHub/deploy status is only wired for authenticated owners.
export const prerender = false;

const isAuthenticated = Boolean(await readAdminSession(Astro.request));

// No demo-mode render: an unauthenticated visitor is sent to the login page.
if (!isAuthenticated) {
  return Astro.redirect("/admin/login");
}
---

<html lang="id">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="robots" content="noindex, nofollow, noarchive" />
    <script is:inline>
      (function () {
        try {
          var stored = window.localStorage.getItem("bsm-theme");
          var theme = stored === "light" || stored === "dark"
            ? stored
            : "light";
          document.documentElement.dataset.theme = theme;
        } catch {
          document.documentElement.dataset.theme = "light";
        }
      })();
    </script>
    <title>Kontrol Editorial BSM · CMS</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Archivo:wdth,wght@62.5..125,400..800&family=Hanken+Grotesk:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500;700&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <CmsDashboard
      client:only="react"
      mediaAssets={mediaAssets()}
      isAuthenticated={isAuthenticated}
    />
  </body>
</html>
